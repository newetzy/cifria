---
import CategoryPage from '../../components/CategoryPage.astro';
import Layout from '../../layouts/Layout.astro';
import { categories } from '../../data/categories';
const category = categories.find(({ slug }) => slug === 'prestamos')!;
---
<Layout title="Calculadoras de préstamos personales | Cifria" description="Calculadoras para estimar cuotas, intereses y coste total de préstamos a tipo fijo."><CategoryPage category={category} /></Layout>
