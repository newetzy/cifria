---
import Layout from '../../layouts/Layout.astro'; import CategoryPage from '../../components/CategoryPage.astro'; import { categories } from '../../data/categories';
const category = categories.find(({ slug }) => slug === 'prestamos')!;
---
<Layout title="Calculadoras de préstamos | Cifria" description="Calculadoras de cuotas, intereses y coste de préstamos."><CategoryPage {category} /></Layout>
