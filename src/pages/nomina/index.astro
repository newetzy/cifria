---
import Layout from '../../layouts/Layout.astro'; import CategoryPage from '../../components/CategoryPage.astro'; import { categories } from '../../data/categories';
const category = categories.find(({ slug }) => slug === 'nomina')!;
---
<Layout title="Calculadoras de nómina | Cifria" description="Calculadoras de nómina y salario para particulares en España."><CategoryPage {category} /></Layout>
