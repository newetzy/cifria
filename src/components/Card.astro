---
export interface Props { as?: 'article' | 'div'; class?: string; }
const { as: Tag = 'article', class: className = '' } = Astro.props as Props;
---
<Tag class:list={['card', className]}><slot /></Tag>
