import { calculateIrpf, type IrpfInput } from '../lib/calculations/irpf'; import { formatEuro } from '../lib/formatters/currency';
const form = document.querySelector<HTMLFormElement>('#irpf-form'); if (!form) throw new Error('No se encontró el formulario de IRPF.');
const results = document.querySelector<HTMLElement>('#irpf-results')!; const announcement = document.querySelector<HTMLElement>('#irpf-announcement')!;
const errorMessage = document.querySelector<HTMLElement>('#irpf-error')!;
const readNumber = (id: string) => form.querySelector<HTMLInputElement>(`#${id}`)!.valueAsNumber;
form.addEventListener('submit', (event) => { event.preventDefault(); errorMessage.hidden = true; errorMessage.textContent = ''; announcement.textContent = ''; try { const input: IrpfInput = { salary: readNumber('salary'), otherIncome: readNumber('otherIncome'), savingsIncome: readNumber('savingsIncome'), socialContributions: readNumber('socialContributions'), pensionContributions: readNumber('pensionContributions'), withheld: readNumber('withheld'), age: readNumber('age'), children: readNumber('children'), childrenUnder3: readNumber('childrenUnder3'), dependantsOver65: readNumber('dependantsOver65'), disability: form.querySelector<HTMLSelectElement>('#disability')!.value as IrpfInput['disability'], filing: form.querySelector<HTMLSelectElement>('#filing')!.value as IrpfInput['filing'] }; const result = calculateIrpf(input); for (const [key, value] of Object.entries({ tax: result.estimatedTax, balance: Math.abs(result.balance), base: result.generalBase, savings: result.savingsBase, minimum: result.personalMinimum })) document.querySelector<HTMLElement>(`[data-result="${key}"]`)!.textContent = formatEuro(value); document.querySelector<HTMLElement>('[data-result="balance-label"]')!.textContent = result.balance > 0 ? 'Saldo simulado a ingresar' : result.balance < 0 ? 'Saldo simulado a devolver' : 'Saldo simulado sin diferencia'; results.hidden = false; announcement.textContent = `Cuota simulada: ${formatEuro(result.estimatedTax)}. Sin cálculo autonómico ni foral.`; } catch (error) { results.hidden = true; errorMessage.textContent = error instanceof Error ? error.message : 'No se ha podido calcular.'; errorMessage.hidden = false; } });
form.requestSubmit();
